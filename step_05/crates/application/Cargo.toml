[package]
name = "application"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
shared = { path = "../shared" }
domain = { path = "../domain" }

[dev-dependencies]
